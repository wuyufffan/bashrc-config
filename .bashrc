# .bashrc - Bash 配置文件
# my_linux_config 生态系统的一部分
# https://github.com/wuyufffan/bashrc-config

# ==============================================================================
# 🎨 ANSI 颜色定义
# ==============================================================================
export RESET='\033[0m'       # 重置/无色
export BOLD='\033[1m'        # 加粗

export RED='\033[1;31m'      # 🔴 亮红
export GREEN='\033[1;32m'    # 🟢 亮绿
export YELLOW='\033[1;33m'   # 🟡 亮黄
export BLUE='\033[1;34m'     # 🔵 亮蓝
export GREY='\033[0;37m'     # ⚪ 灰色
export PURPLE='\033[1;35m'   # 🟣 亮紫
export CYAN='\033[1;36m'     # 🔵 亮青

# ==============================================================================
# 🕒 时间函数
# ==============================================================================
export TIME_STYLE_COLOR="${GREY}"

# 简短时间格式 [12:30:59]
function now() {
    echo -e "${TIME_STYLE_COLOR}[$(date +'%H:%M:%S')]${RESET}"
}

# 完整时间格式 [2024-05-20 12:30:59]
function timestamp() {
    echo -e "${TIME_STYLE_COLOR}[$(date +'%Y-%m-%d %H:%M:%S')]${RESET}"
}

# ==============================================================================
# 1. 加载系统默认配置
# ==============================================================================
if [ -f /etc/bashrc ]; then
  . /etc/bashrc
fi

# ==============================================================================
# 2. 显示节点信息
# ==============================================================================
HOST_NAME=$(hostname)
echo -e "🖥️  节点: ${BLUE}${HOST_NAME}${RESET} $(timestamp)"

# ==============================================================================
# 3. 组件扩展支持（可选）
# 其他组件可以将初始化脚本放在以下目录：
# ~/.config/my_linux_config/components/
# ==============================================================================
COMPONENTS_DIR="${HOME}/.config/my_linux_config/components"
if [ -d "${COMPONENTS_DIR}" ]; then
    for script in "${COMPONENTS_DIR}"/*.sh; do
        [ -f "$script" ] && source "$script"
    done
fi

# ==============================================================================
# 4. 别名定义
# ==============================================================================

# --- 系统安全别名 ---
alias rm='rm -i'      # 删除前确认
alias cp='cp -i'      # 覆盖前确认
alias mv='mv -i'      # 移动前确认

# --- 常用工具别名 ---
alias c='clear'       # 清屏
alias h='history'     # 显示历史
alias bashrc='source ~/.bashrc && echo "✅ bashrc 已重载 $(timestamp)"'
alias ll='ls -alF --color=auto'   # 详细列表
alias ls='ls --color=auto'        # 彩色列表
alias tree='tree -C'              # 彩色树形显示

# ==============================================================================
# 5. 欢迎信息
# ==============================================================================
echo -e "${GREEN}✅ Bash 配置已加载${RESET}"
